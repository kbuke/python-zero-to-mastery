# __name__
    # You will come across this code often
        # if __name__ = '__main__'
    # __name__ is a Dunder Method , and a file that is being run
    # If I ran this file, it would equal __main__, other files not run still have names, but not __main__