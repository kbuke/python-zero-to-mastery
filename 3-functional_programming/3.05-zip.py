# Zip
    # Uses same logic as map and filter
    # Zips to iterable objects together in Python

your_list = [1, 2, 3]
my_list = [4, 5, 6]

print(list(zip(your_list, my_list))) #[(1, 4), (2, 5), (3, 6)]