# Used to do things within string quotations
    # \ => states everything after the backslash is still a string
weather = 'It\'s kind of sunny'

    # \t => adds a tab to the string (an indent)
t_text = '\t This text is indented'
print(t_text)

    # \n => can seperate to a new line
new_line = "1st line\n2nd line"
print(new_line)