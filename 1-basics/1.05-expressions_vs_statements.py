# Expressions are a value
# Statements are an entire line of code
iq = 100
user_age = iq / 5
# In the above two lines:
    # 100 is an expression
    # 20 is an expression (100 / 5)
    # lines 3 and 4 are statements
